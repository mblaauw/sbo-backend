
services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_USER: sbo_user
      POSTGRES_PASSWORD: sbo_password
      POSTGRES_DB: sbo_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  skills-service:
    build:
      context: .
      dockerfile: ./services/skills_service/Dockerfile
    environment:
      - DATABASE_URL=postgresql://sbo_user:sbo_password@postgres:5432/sbo_db
      - LLM_SERVICE_URL=http://llm-service:8105
    ports:
      - "8101:8100"
    depends_on:
      - postgres

  matching-service:
    build:
      context: .
      dockerfile: ./services/matching_service/Dockerfile
    environment:
      - DATABASE_URL=postgresql://sbo_user:sbo_password@postgres:5432/sbo_db
      - SKILLS_SERVICE_URL=http://skills-service:8100
      - USER_SERVICE_URL=http://user-service:8100
    ports:
      - "8102:8100"
    depends_on:
      - postgres
      - skills-service

  user-service:
    build:
      context: .
      dockerfile: ./services/user_service/Dockerfile
    environment:
      - DATABASE_URL=postgresql://sbo_user:sbo_password@postgres:5432/sbo_db
      - SKILLS_SERVICE_URL=http://skills-service:8100
      - ASSESSMENT_SERVICE_URL=http://assessment-service:8100
    ports:
      - "8103:8100"
    depends_on:
      - postgres

  assessment-service:
    build:
      context: .
      dockerfile: ./services/assessment_service/Dockerfile
    environment:
      - DATABASE_URL=postgresql://sbo_user:sbo_password@postgres:5432/sbo_db
      - SKILLS_SERVICE_URL=http://skills-service:8100
      - LLM_SERVICE_URL=http://llm-service:8100
    ports:
      - "8104:8100"
    depends_on:
      - postgres
      - skills-service
      - llm-service

  llm-service:
    build:
      context: .
      dockerfile: ./services/llm_service/Dockerfile
    environment:
      - DATABASE_URL=postgresql://sbo_user:sbo_password@postgres:5432/sbo_db
      - SKILLS_SERVICE_URL=http://skills-service:8100
      - LLM_API_KEY=${LLM_API_KEY:-dummy-key}
    ports:
      - "8105:8100"
    depends_on:
      - postgres

  api-gateway:
    build:
      context: .
      dockerfile: ./services/api_gateway/Dockerfile
    environment:
      - SKILLS_SERVICE_URL=http://skills-service:8100
      - MATCHING_SERVICE_URL=http://matching-service:8100
      - USER_SERVICE_URL=http://user-service:8100
      - ASSESSMENT_SERVICE_URL=http://assessment-service:8100
      - LLM_SERVICE_URL=http://llm-service:8100
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-dev_secret_key}
    ports:
      - "8101:8101"
    depends_on:
      - skills-service
      - matching-service
      - user-service
      - assessment-service
      - llm-service

volumes:
  postgres_data: